reflar-oauth-gitlab:

  admin:
    gitlab_settings:
      title: GitLab Settings
      client_id_label: Application ID
      client_secret_label: Secret

  forum:
    log_in:
      with_gitlab_button: Login with GitLab